// Converts an array into a linked list

let listToArray = (arr) => {
  let list = {
    value: arr[0],
    rest: null
  }

  for(let i = 1; i < arr.length; ++i){
    let 


  }

  console.log(list);
}

listToArray([1, 2, 3]);